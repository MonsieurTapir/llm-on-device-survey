# Build the benchmark report from submissions/ and deploy it as the
# GitHub Pages site on every push to main. The report is never committed:
# each merge triggers a fresh build over all submissions, so submission PRs
# only ever add their own folder and can land in parallel.
# One-time setup: repo Settings → Pages → Source: "GitHub Actions".
name: pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v7
      - uses: astral-sh/setup-uv@v9.0.0
      - name: Build the report as the site root
        # Always built fresh over submissions/ (empty-state page when
        # no submission has landed yet).
        run: |
          mkdir -p site
          uv run --group analysis python -m analysis.site --out site/index.html
      - uses: actions/upload-pages-artifact@v5
        with:
          path: site
      - id: deployment
        uses: actions/deploy-pages@v5
